apiVersion: v1
kind: Service
metadata:
  name: local-registry
  namespace: kube-system
spec:
  type: NodePort
  selector:
    app: local-registry
  ports:
    - port: 5000
      targetPort: 5000 
      nodePort: 30069 
      protocol: TCP
      name: registry
